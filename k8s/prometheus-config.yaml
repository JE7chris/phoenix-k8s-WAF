apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
data:
  prometheus.yml: |
    global:
      scrape_interval: 5s # 每 5 秒抓一次

    scrape_configs:
      - job_name: 'phoenix-app'
        static_configs:
          - targets: ['phoenix-service:80'] # 👈 你的 Service 名字
